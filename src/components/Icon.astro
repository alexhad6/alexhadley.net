---
type Props = {
  /** SVG `viewBox`. */
  viewBox: string;
  /** SVG path to render. */
  svgPath: string;
};

const { svgPath, viewBox } = Astro.props;
---

<svg class="h-6 w-6" {viewBox} aria-hidden="true">
  <path d={svgPath}></path>
</svg>
