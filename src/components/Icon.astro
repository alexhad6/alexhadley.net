---
type Props = {
  /** SVG `viewBox`. */
  viewBox: string;
  /** SVG path to render. */
  svgPath: string;
};

const { svgPath, viewBox } = Astro.props;
---

<svg class="h-7 w-7 fill-current" {viewBox} aria-hidden="true">
  <path d={svgPath}></path>
</svg>
